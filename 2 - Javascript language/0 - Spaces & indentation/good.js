var test = 'pf';                            // Space before and after =
                                            // Add a line between your different code parts
function test() {                           // Space between function keyword and the name of the function itself - Space between function name and the parenthesis.
    return {                                // Indent with four or 2 spaces more on each new block.
        test: 'test'                        // Space after : in objects.
    };
}

function () {                               // Space between function keyword and the parenthesis if its an anonymous function
    console.log('hei');                     // Space after opening parenthesis and before closing parenthesis in function calls.
}

function test2(argument1, argument2) {      // Space between function keyword and the name of the function itself - Space
    return argument1 + argument2;
}

if (test === 'pf') {
    console.log('pf');
}

for (var i = 0; i < 10; i++) {

}